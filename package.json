{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "mocha --reporter spec",
    "tc": "nyc --reporter=text mocha",
    "tc90": "nyc --check-coverage --lines 90 --reporter=text mocha",
    "tc90-per-file": "nyc --check-coverage --lines 90 --per-file --reporter=text mocha",
    "start": "nodemon --legacy-watch  ./bin/www",
    "start:normal": "nodemon ./bin/www"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "mongoose": "^5.7.5",
    "morgan": "~1.9.1",
    "saslprep": "^1.0.3"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^6.2.0",
    "nodemon": "^1.19.1",
    "nyc": "^14.1.1",
    "sinon": "^7.4.2",
    "sinon-test": "^2.4.0",
    "supertest": "^4.0.2"
  }
}
